apiVersion: v1
kind: Service
metadata:
  name: product-preview-card-component-main
  namespace: default
spec:
  selector:
    app: product-preview-card-component-main-app
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
